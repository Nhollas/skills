{
  "skill_name": "frontend-testing",
  "evals": [
    {
      "id": 1,
      "prompt": "Write browser tests for this UserList component that fetches and displays team members. The component is at evals/files/UserList.tsx.",
      "expected_output": "A test file using Vitest browser mode with MSW handlers, page object, fixtures, factory functions, and role-based locators",
      "files": ["evals/files/UserList.tsx"],
      "expectations": [
        "Uses getByRole() for locating elements, not CSS selectors or data-testid",
        "Uses browserWorker.use() for per-test API handlers",
        "Page object uses Object.assign(page, self) pattern",
        "Page object methods follow get*/expect*/action naming convention",
        "Uses test.extend<T>() for typed fixtures",
        "Test names describe observable user behavior, not implementation details",
        "Uses a factory function (e.g. createUser()) with overridable defaults for test data",
        "User item locators scope within the list region (not querying page directly)",
        "MSW handlers are extracted into separate reusable handler functions"
      ]
    },
    {
      "id": 2,
      "prompt": "This TaskBoard component needs tests — it has filtering by priority and three status columns (todo, in-progress, done). The component is at evals/files/TaskBoard.tsx.",
      "expected_output": "A test file with scoped locators for different columns, dynamic aria-label handling, page object with scoped locators, and fixture composition",
      "files": ["evals/files/TaskBoard.tsx"],
      "expectations": [
        "Locators chain through parent regions to scope queries within different columns",
        "Suggests semantic markup improvements (e.g. section with aria-label instead of div)",
        "Suggests dynamic aria-label for task cards based on their content or status",
        "Page object uses self-referencing scoped locators (self.getColumn() pattern)",
        "Fixtures use composition with dependency declarations or auto: true"
      ]
    },
    {
      "id": 3,
      "prompt": "I need to add tests for this ContactForm — it validates inputs client-side and submits to /api/contacts. The component is at evals/files/ContactForm.tsx.",
      "expected_output": "A test file with MSW custom resolvers matching request body, per-test overrides for error scenarios, form role locators, and behavior-focused test names",
      "files": ["evals/files/ContactForm.tsx"],
      "expectations": [
        "Uses a custom higher-order resolver (e.g. withJsonBody) for POST request body matching",
        "Uses browserWorker.use() to override handlers for error scenarios",
        "Uses role-based locators for form elements (textbox, button roles)",
        "Test names describe validation and submission behaviors, not implementation details"
      ]
    },
    {
      "id": 4,
      "prompt": "Write tests for this NotificationPanel — it shows a list of notifications with read/unread status, has a bell icon with a badge count, and auto-scrolls to new notifications. The component is at evals/files/NotificationPanel.tsx.",
      "expected_output": "A test file that hides decorative icons with aria-hidden, uses data-testid only for the scroll container state, computes dynamic aria-labels for notification items, and uses factory functions for notification data",
      "files": ["evals/files/NotificationPanel.tsx"],
      "expectations": [
        "Suggests or uses aria-hidden=\"true\" for decorative icons (BellIcon, CheckIcon)",
        "Uses data-testid only for the scroll container's non-visible state (not for locating regular UI elements)",
        "Suggests dynamic aria-label for notification items based on content (e.g. notification title)",
        "Suggests semantic HTML elements instead of plain divs for notification structure",
        "Uses a factory function (e.g. createNotification()) with overridable defaults for test data"
      ]
    },
    {
      "id": 5,
      "prompt": "Write tests for this Dashboard component. It has a sidebar with navigation and a main content panel — both contain lists and headings that could clash. I want separate fixture files for the sidebar and main content areas. The component is at evals/files/Dashboard.tsx.",
      "expected_output": "Multiple files: separate fixture files per feature area, page objects with scoped locators for sidebar vs main content, and fixture composition with dependency declarations",
      "files": ["evals/files/Dashboard.tsx"],
      "expectations": [
        "Creates separate fixture files for sidebar and main content areas",
        "Sidebar page object scopes locators within the sidebar region",
        "Main content page object scopes locators within the main content region",
        "Locators chain through parent regions to avoid cross-region clashes between lists/headings",
        "Fixtures use composition with dependency declarations"
      ]
    },
    {
      "id": 6,
      "prompt": "I need to set up vitest for this React project. We need browser tests using Playwright and unit tests that run in Node. Can you set up the vitest config, the browser test setup file with MSW, and show me where test files should go? The project scaffold is at evals/files/project-scaffold/.",
      "expected_output": "A vitest config with dual projects (unit + browser), a browser setup file with MSW worker start/reset/stop, and guidance on test file colocation",
      "files": [
        "evals/files/project-scaffold/package.json",
        "evals/files/project-scaffold/tsconfig.json",
        "evals/files/project-scaffold/src/components/counter.tsx",
        "evals/files/project-scaffold/src/utils/format.ts"
      ],
      "expectations": [
        "Vitest config has two projects: unit (Node, *.test.ts) and browser (Playwright, *.test.tsx)",
        "Browser setup file starts MSW worker with onUnhandledRequest: \"error\"",
        "Setup file has resetHandlers() in afterEach and stop() in afterAll",
        "Test files are colocated with source files, not in a separate __tests__ directory"
      ]
    }
  ]
}
